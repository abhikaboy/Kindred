import { Dimensions } from "react-native";

export const HORIZONTAL_PADDING = Dimensions.get("window").width * 0.05;
export const VERTICAL_PADDING = Dimensions.get("window").height * 0.03;
export const DRAWER_WIDTH = Dimensions.get("screen").width * 0.85;
